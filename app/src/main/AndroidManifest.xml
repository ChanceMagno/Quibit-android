<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epicodus.quibit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchRewards">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.epicodus.quibit.MainActivity"></meta-data>
        </activity>
        <activity android:name=".CreateGoal">
            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:value="com.epicodus.quibit.ADD PARENT HERE"> -->
            <!-- </meta-data> -->
        </activity>
        <activity android:name=".ItemDetail">
             <meta-data
             android:name="android.support.PARENT_ACTIVITY"
             android:value="com.epicodus.quibit.SearchRewards">
             </meta-data>
        </activity>
    </application>

</manifest>